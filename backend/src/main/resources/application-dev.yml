spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vietmoney_db?useSSL=false&serverTimezone=UTC
    username: root
    password: taido231105
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # dev dùng update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false   # tạm tắt nếu chưa dùng migration

logging:
  level:
    com.vietmoney: DEBUG